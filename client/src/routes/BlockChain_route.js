import BlockChainForm from "../components/BlockChainForm";

function BlockChain() {
  return (
    <div>
      <h2 className="toppic">Blockchain</h2>
      <BlockChainForm length={5} />
    </div>
  );
}

export default BlockChain;
